sudo: required

language: generic

services:
  - docker

before_install:
- echo "docker compose creating and start"
- docker compose build

script:
- docker compose up

after_success:
- echo "Test Success"